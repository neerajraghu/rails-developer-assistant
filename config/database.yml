# PostgreSQL.
#
# Install the pg driver:
#   gem install pg
#
default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  host: <%= ENV.fetch("POSTGRES_HOST", "localhost") %>
  port: <%= ENV.fetch("POSTGRES_PORT", 5432) %>
  username: <%= ENV.fetch("POSTGRES_USER", "postgres") %>
  password: <%= ENV.fetch("POSTGRES_PASSWORD", "postgres") %>

development:
  <<: *default
  database: rails_developer_assistant_development

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  <<: *default
  database: rails_developer_assistant_test

# In production, Render provides a full connection string via the DATABASE_URL
# environment variable (set automatically when you link a Render Postgres
# database to this web service). Rails merges it on top of these defaults
# automatically, so no host/username/password needs to be repeated here.
#
# A single database, no separate primary/cache/queue/cable roles: Rails only
# auto-applies DATABASE_URL to a role named "primary", any other named role
# falls back to the plain defaults above and fails to connect. This app has no
# background jobs and doesn't need those split out anyway.
production:
  <<: *default
